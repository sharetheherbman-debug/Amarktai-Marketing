# Copy to the repository root as .env for Docker Compose interpolation.
# Do not commit the copied .env file.

COMPOSE_PROJECT_NAME=amarktai_marketing
POSTGRES_PASSWORD=replace-with-a-strong-password
DOMAIN=marketing.amarktai.com
VITE_API_URL=/api/v1
